# Drawable e Resources

### Drawable

* É um recurso gráfico

* Pode ser recuperado pela seguinte forma

  * **Método**: `ResourcesCompat.getDrawable(...)`

  * **Atributo**: `android:drawable` e `android:icon`

* Tipo mais comum de drawable: arquivo bitmap (`.jpg`, `.png` ou `.gif`)