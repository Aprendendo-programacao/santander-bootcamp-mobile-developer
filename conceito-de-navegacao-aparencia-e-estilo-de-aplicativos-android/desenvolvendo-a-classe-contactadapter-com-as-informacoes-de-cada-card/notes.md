# Desenvolvendo a classe ContactAdapter com as informações de cada card

### `notifyDataSetChanged()`

* Notificar o *Adapter* que a lista utilizada, por ele, na renderização foi alterada e com isso o *Adapter* irá refazer o *binding* de cada elemento novamente (`onBindViewHolder()`)